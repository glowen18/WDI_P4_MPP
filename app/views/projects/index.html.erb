<% @projects.each do |project| %>
  <div class="index_row clearfix">
    <h3 class="project_title"><%= link_to project.title, project %></h3>
  </div>
<% end %>

<div class="links">
  <%= link_to "New Project", new_project_path %>
</div>
